export type AnnonceApiModel = {
    id: string;
    text: string;
    publication: string;
    expires: string;
    signature: string;
    userId: number;
    userName: string;
    isAlert: boolean;
    addedAt: string;
    addedBy: string;
    addedByName: string;
    updatedAt: string;
    updatedBy: string;
};
